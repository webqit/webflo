{
  "title": "Webflo Test",
  "type": "module",
  "scripts": {
    "generate": "webflo generate::client --recursive --compression --auto-embed",
    "bundle": "cd public && oohtml bundle",
    "build": "npm run generate && npm run bundle"
  }
}
